import 'package:flutter/material.dart';

class RecordPlayersList extends StatelessWidget {
  const RecordPlayersList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('test-further'),
    );
  }
}
